
#include <bits/stdc++.h>
using namespace std;

void Matrix_rotate_by_90(vector<vector<int>> arr(int n)) {

    int n = arr.size();
    
    for (int i = 0;i < n;i++ ) {

        for(int j = 0; j < n; j++) {

            
        }
    }
}



int main() {

    int n,m;

    cin >> n >> m;

    vector<vector<int>> arr(n);

    for (int i = 0; i < n; i++) {

        for (int j = 0; j < m; j++) {

            cin >> arr[i][j];
       }
    }
}